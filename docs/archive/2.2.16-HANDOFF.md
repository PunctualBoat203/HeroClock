# HeroClock development handoff

Author: PunctualBoat. Current development version: **2.2.16** on `improve-clock-runtime`.

This pass continues the pushed 2.2.15 pause checkpoint `d66a66d`, preserving its persistent timers, bounded work, Palladium property lookup cache, Satsu redirect, Curios view reuse and public companion handshake. Historical artifact hashes, Spark findings and 2.2.15 validation are retained in [the archived handoff](archive/2.2.15-HANDOFF.md).

## Current validated release candidate

Current validated code: `bd8a777f784451c5e7163a03115c2b247d2cd921`

CurseForge/runtime artifact: `HeroClock-2.2.16.jar`

SHA-256:

```
7064676767913c3b98b6d77a5582b526be48b47eac33c18c37bdb8efe81c8eab
```

GitHub Actions run `35657584624` passed the full validation matrix for this exact commit:

- unit tests and reobfuscated build;
- API/runtime packaging verification;
- base Forge GameTests;
- Palladium + Curios Forge GameTests;
- unchanged-code/different-version compatibility acceptance;
- intentionally changed-target compatibility rejection/fail-open behavior;
- artifact upload.

The main runtime JAR now contains an inert, extractable developer API copy at:

`META-INF/heroclock/HeroClock-2.2.16-api.jar`

CI verifies that the embedded API JAR is byte-for-byte identical to the standalone compile-only API artifact and that no Forge JarJar metadata is present. The embedded API is therefore a developer convenience, not a second loaded mod or nested runtime dependency.

The normal runtime JAR remains the only file players should install. Java addon developers may extract the embedded API JAR and use it as a compile-only dependency. KubeJS users can access the same public `com.heroclock.api.*` classes directly from the installed runtime JAR and do not need to extract the API artifact.

## Current release scope

This 2.2.16 release candidate intentionally contains the validated HeroClock/Palladium/Curios/Satsu/runtime work only. It does **not** include the unfinished universal KubeJS/Rhino optimization pass.

Active scope includes:

- persistent HeroClock timers/deadlines;
- bounded/coalesced deferred work;
- lifecycle-driven temporary-helper cleanup;
- Palladium compatibility-contract gating, property lookup caching, unchanged scalar sync suppression, power-holder view reuse and command cache invalidation;
- Curios read-only map-view reuse with backing-map identity tracking;
- verified Satsu sentinel cleanup redirect;
- OmniOptimizer companion handshake support, including the current API handshake path intended for OmniOptimizer 1.8.2;
- API v1 for timers, bounded work, deferred functions and diagnostics;
- datapack commands for timers, deferred functions and compatibility/status access.

Do not silently add universal KubeJS/Rhino engine changes to the CurseForge 2.2.16 artifact. Those changes belong in a separately validated follow-up/scripting pass.

## Latest real-pack performance checkpoint

A newer real-pack Spark capture after the 2.2.16 work still shows substantial tick pressure, but HeroClock itself remains negligible in the sampled hot path.

Observed ending statistics from that capture were approximately:

- 17.06 TPS over the last minute;
- 15.49 TPS over the last five minutes;
- 53.4 ms mean / 47.6 ms median / 84.4 ms p95 MSPT over the last minute;
- 55.6 ms mean / 50.3 ms median / 87.1 ms p95 MSPT over the last five minutes.

The remaining superhero-stack hotspots are concentrated in:

- Minecraft command/function execution and entity selectors;
- Palladium ability/condition evaluation;
- Rhino/interpreted script execution;
- repeated Palladium property/NBT work.

HeroClock's own sampled exclusive work remained effectively negligible relative to active server tick time.

Interpretation for the next pass:

- do not spend time micro-optimizing HeroClock's scheduler itself unless a future profile changes that picture;
- the highest-value remaining work is reducing redundant script/command/callback execution around Palladium/KubeJS/Rhino;
- use instrumentation before adding broad caches or callback suppression;
- preserve behavior and side effects; do not globally throttle arbitrary script callbacks;
- Pehkui and Curios should only receive additional work if profiling identifies a superhero-driven access pattern that their own caches do not already cover.

## Next development direction / scripting handoff

The next major optimization phase is the universal KubeJS/Rhino work planned for scripting. Keep it isolated from this validated 2.2.16 release baseline.

Recommended order:

1. Add instrumentation for Java↔Rhino/KubeJS callback frequency, repeated property/member resolution and command/function invocations.
2. Separate script-engine overhead from work triggered by the scripts (Palladium checks, selectors, NBT serialization, commands).
3. Optimize only deterministic/redundant boundary work first.
4. Avoid caching arbitrary JavaScript results or suppressing callbacks that may have gameplay side effects.
5. Re-run the same representative Spark workload after each isolated optimization.
6. Keep HeroClock focused on the superhero/Palladium/KubeJS/scoreboard/callback ecosystem rather than unrelated general-modpack hotspots.
7. Preserve `bd8a777f...` / SHA-256 `70646767...` as the clean pre-universal-KubeJS/Rhino comparison baseline.

## Current changes

- Optional mixins are selected separately by audited target-method fingerprints and required fields, replacing exact-version allowlists. Missing/changed targets retain base behavior. Version metadata remains context for historical resource baselines.
- Satsu's actual loaded function is validated and rechecked on reload, including before queued cleanup executes.
- Curios view reuse follows backing-map identity. Palladium lookup caching invalidates on registration/reload and yields to live lookups after the mutable values map is exposed.
- API v1 exposes timers, bounded jobs, deferred functions and immutable diagnostic snapshots. CI still produces a standalone compile-only API JAR, and the exact same API JAR is now embedded inertly at `META-INF/heroclock/HeroClock-2.2.16-api.jar` inside the runtime mod so developers can extract it from the distributed HeroClock JAR. It is not registered as a Forge JarJar dependency.
- Datapack commands provide timer and function-work access without Java integration.
- HeroClock uses PunctualBoat attribution. The companion handshake discovers its API from the installed entrypoint package instead of hardcoding a vendor namespace.

## Validation boundaries

Unit tests cover compatibility acceptance/rejection alongside clock and work-queue edge cases. CI builds a reobfuscated runtime JAR, verifies that the embedded API is byte-for-byte identical to the standalone API artifact and remains non-JarJar, then runs Forge GameTests. Target-pack save/reload, equipment, client sync and performance checks remain necessary before a release. A structural match does not prove every interaction with other mods safe. Static historical resource overrides are not automatically authorized for arbitrary addon updates by the Java compatibility guards.

See [compatibility](COMPATIBILITY.md), [integration](INTEGRATION.md) and [validation](VALIDATION.md). Keep commits incremental and retain stock behavior when a new optimization cannot prove its own prerequisites. Do not broadly throttle arbitrary script callbacks or cache mutable gameplay results.
