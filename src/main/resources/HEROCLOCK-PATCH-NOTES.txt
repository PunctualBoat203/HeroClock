HeroClock 2.2.16 — PunctualBoat

Optional Palladium and Curios optimizations check audited method bodies and required fields instead of exact version labels. Incompatible targets keep base behavior independently.
Satsu validates the effective loaded function and rechecks after reload.
Property lookups invalidate on registration/reload and stop caching after mutable storage is exposed. Curios views track backing-map replacement.
API v1 provides persistent timers, bounded work, deferred functions and immutable diagnostics through a separate compile-only API artifact.
Datapack commands expose timer operations, keyed deferred functions and compatibility status.
Retains persistent game-tick deadlines, cooperative work budgets and targeted lifecycle cleanup.

Requires target-pack persistence, gameplay and performance validation before production use.
