HeroClock 2.2.17 — PunctualBoat

KubeJS listener registration resumes appending from the last known tail, preserving handler order and dispatch.
Rhino Java-map enumeration fills the returned ID array directly, avoiding the intermediate list and copy.
Both optimizations use audited code/field contracts; changed targets retain stock behavior independently.
Optional guarded Rhino/KubeJS boundary counters and timings are disabled by default.
Embedded HeroScriptAPI provides bounded iterator batches, namespace cancellation and immutable profiling snapshots.
Retains the validated 2.2.16 runtime and inert embedded compile-only API packaging.

No arbitrary script callbacks are skipped, delayed or cached. Native KubeJS scheduling is unchanged.
Target-pack before/after performance validation remains required.
